name: 🚀 UI Tests
run-name: 🚀 Test ${{ github.event.inputs.ENV }} | ${{ github.event.inputs.MARKER }} marker | ${{ github.event.inputs.THREADS }} threads

on:
  workflow_dispatch:
    inputs:
      ENV:
        description: "Выберите окружение"
        required: true
        type: environment

      MARKER:
        description: "Выберите область для тестов"
        required: true
        default: "smoke"
        type: choice
        options:
          - smoke
          - login
          - change_role

      BROWSER:
        description: "Выберите браузер"
        required: true
        default: "chrome"
        type: choice
        options:
          - chrome
          - firefox
          - safari

      THREADS:
        description: "Выберите количество потоков"
        required: false
        type: choice
        default: "1"
        options:
          - "3"
          - "5"
          - "10"
jobs:
  ui-tests:
    runs-on: ubuntu-latest
    # Используем Docker-образ из Docker Hub как окружение
    container:
      image: topanovda/pl-ui:v1   # ← твой образ из Docker Hub
      options: --shm-size=2g      # важно для браузерных тестов (Chromium/Firefox)

    env:
      STAGE: ${{ github.event.inputs.ENV }}
      BROWSER: ${{ github.event.inputs.BROWSER }}
      SUITE: ${{ github.event.inputs.MARKER }}
      THREADS: ${{ github.event.inputs.THREADS }}
    environment: ${{ inputs.ENV }}  # ✅ Привязка джоба к выбранному environment

    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: ./.github/gh-pages
            
      # Подготовка environment.properties
      - name: Prepare Allure environment
        run: |
          mkdir -p allure-results
          echo "Branch=${{ github.ref_name }}" > allure-results/environment.properties
          echo "Commit=${{ github.sha }}" >> allure-results/environment.properties
          echo "Browser=${{ inputs.BROWSER }}" >> allure-results/environment.properties
          echo "Stage=${{ inputs.ENV }}" >> allure-results/environment.properties

      # Запуск тестов
      - name: Run tests via docker-compose
        env:
          STAGE: ${{ secrets.STAGE }}
          BROWSER: ${{ inputs.BROWSER }}
          SUITE: ${{ inputs.MARKER }}
          THREADS: ${{ inputs.THREADS }}
        run: docker compose up pl-ui

      # Шаг 4 - Копирование истории
      - name: Copy Allure history
        run: |
          mkdir -p allure-results/history
          if [ -d "./.github/gh-pages/history" ]; then
          cp -R ./.github/gh-pages/history/* allure-results/history/
          fi

      # Шаг 5 - Генерация отчета
      - name: Generate Allure-report
        env:
          BROWSER: ${{ inputs.BROWSER }}
          STAGE: ${{ secrets.STAGE }}
          CI_COMMIT_REF_NAME: ${{ github.ref_name }}
          CI_COMMIT_SHORT_SHA: ${{ github.sha }}
        run: docker compose run --rm report

      - name: Debug allure folders
        run: |
          echo "=== allure-results ==="
          ls -la allure-results || true
          echo "=== allure-report ==="
          ls -la allure-report || true

      # Шаг 6 - Деплой на gh-pages
      - name: Deploy to Github Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.CI_TOKEN }}
          branch: gh-pages
          folder: allure-report
          clean: false